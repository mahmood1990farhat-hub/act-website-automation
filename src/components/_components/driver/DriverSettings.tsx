import React from 'react'
import { Locale } from '../../../../i18n.config';

export default function DriverSettings({
  token,
  locale,
  trans,
}: {
  token?: string;
  locale: Locale;
  trans: any;
}) {
  return (
    <div>DriverSettings</div>
  )
}
