export enum Directions {
  RTL = "rtl",
  LTR = "ltr",
}

export enum Languages {
  ENGLISH = "en",
  ARABIC = "ar",
}

// Lost property enums
export enum LostPropertyItemTypes {
  LUGGAGE = "luggage",
  BAG = "bag",
  WALLET = "wallet",
  PHONE = "phone",
  KEYS = "keys",
  DOCUMENTS = "documents",
  CLOTHING = "clothing",
  OTHER = "other",
}

export enum LostPropertyContactPreferences {
  PHONE = "phone",
  EMAIL = "email",
  SMS = "sms",
}

export enum LostPropertyStatus {
  REPORTED = "reported",
  UNDER_INVESTIGATION = "under_investigation",
  FOUND = "found",
  RETURNED = "returned",
  NOT_FOUND = "not_found",
  CLOSED = "closed",
}


